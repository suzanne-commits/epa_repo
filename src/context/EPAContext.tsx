// Re-export shim: the real implementation lives at src/EPAContext.tsx (do not modify).
// The rest of the scaffold (App.tsx, main.tsx, view stubs) imports from this path.
export * from '../EPAContext'
